{
	"name": "@braingame/config",
	"version": "1.0.0",
	"description": "Brain Game shared configuration - ESLint, TypeScript, Biome presets",
	"main": "index.js",
	"scripts": {
		"dev": "echo 'Config package ready'",
		"build": "echo 'No build needed for config'",
		"lint": "echo 'Config package ready'",
		"typecheck": "echo 'No typecheck needed for config'",
		"test": "echo 'No tests for config'",
		"clean": "echo 'No cleanup needed for config'"
	},
	"files": [
		"*.json",
		"*.js",
		"*.ts"
	],
	"publishConfig": {
		"access": "public"
	}
}
