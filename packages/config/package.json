{
	"name": "@braingame/config",
	"version": "1.0.0",
	"description": "BrainGame shared configuration - ESLint, TypeScript, Biome presets",
	"main": "index.js",
	"scripts": {
		"dev": "echo 'Config package ready'",
		"build": "echo 'No build needed for config'",
		"lint": "echo 'Config package ready'"
	},
	"files": ["*.json", "*.js", "*.ts"],
	"publishConfig": {
		"access": "public"
	}
}
